# Permite tudo por padrão. Uma página pode se excluir da indexação via
# <meta name="robots" content="noindex"> própria (ex.: galeria.html) —
# mais confiável por página do que listar um Disallow aqui, e não impede
# o crawler de seguir os links que saem dela.
User-agent: *
Allow: /

# Atualize esta URL quando o domínio próprio entrar (ver docs/DOMINIO.md).
Sitemap: https://pgillets.github.io/simple_single_page_app/sitemap.xml
